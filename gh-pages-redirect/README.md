# GitHub Pages redirect

These files are meant to live on the `gh-pages` branch so that
`standard-librarian.github.io` redirects to the Vercel deployment.

Steps:
1. Create a `gh-pages` branch that contains only `index.html` and `404.html` from this folder.
2. Configure GitHub Pages to serve from the `gh-pages` branch.
